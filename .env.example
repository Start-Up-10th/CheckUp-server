# 로컬 개발용 예시 값. compose.yaml의 postgres·redis와 맞춘다.
# 실제 비밀값은 .env(커밋 제외)에 두고 이 파일에는 넣지 않는다.
DB_URL=jdbc:postgresql://localhost:5433/checkup
DB_USERNAME=checkup
DB_PASSWORD=checkup
REDIS_HOST=localhost
REDIS_PORT=6379
